<ruleset>

  <!-- Get the theme to apply -->
  <!--
    <theme pyref="mars.skin:get_theme" />
    -->

  <!-- Map domains to rules set -->
  <match domain="localhost.com" class="deliverance" />
  <match domain="bis.localhost.com.bis" class="deliverance" />
  <match domain="ter.localhost.com" class="deliverance" />

  <!-- Rules for the mars -->
  <rule class="deliverance">
    <!-- Copy over the contents of the page body -->
    <replace content='//*[@id="content"]' theme='//*[@id="my-content-area"]' />
  </rule>

  <!-- vim:set ft=xml: -->
</ruleset>
